# Pinned to CPU-friendly versions. Torch CPU wheels keep the image small enough
# for free-tier hosts (HF Spaces CPU / Fly.io shared-cpu / Render free).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
transformers==4.48.0
torch==2.5.1
sentencepiece==0.2.0
sacremoses==0.1.1
langdetect==1.0.9
protobuf==5.29.2
psycopg[binary]==3.2.3   # retrain.py: read forward store + write head weights (Neon)
