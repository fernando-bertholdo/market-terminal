# ─── Dependencies ─────────────────────────────────────────────────────────────
node_modules/
.pnp
.pnp.js

# ─── Next.js build output ──────────────────────────────────────────────────────
.next/
out/
build/
dist/

# ─── Environment variables (NEVER commit these) ────────────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ─── Vercel ───────────────────────────────────────────────────────────────────
.vercel

# ─── TypeScript ───────────────────────────────────────────────────────────────
*.tsbuildinfo
next-env.d.ts

# ─── Misc ─────────────────────────────────────────────────────────────────────
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── Editor ───────────────────────────────────────────────────────────────────
.vscode/settings.json
.idea/
.claude/plans/
.claude/settings.json
.claude/settings.local.json
*.swp
*.swo

# ─── Testing ──────────────────────────────────────────────────────────────────
/coverage
/playwright-report
/test-results

# ─── Logs ─────────────────────────────────────────────────────────────────────
logs/
*.log

# simulator paper-trading state
/data/
.vercel

# news ML — Python artifacts & generated datasets (research/news-bootstrap, services/news-nlp)
__pycache__/
*.pyc
research/news-bootstrap/*.jsonl
services/news-nlp/.venv/
services/model-engine/.venv/

# Local Codex session exports
2026-*-this-session-is-being-continued-from-a-previous-c.txt

# Artefatos de runtime de agente — nascem não-rastreados a cada run do daemon;
# ficam fora do versionamento para o pré-voo de working tree limpa nunca bloquear.
# `/CLAUDE.md` e `/AGENTS.md` NÃO entram aqui: os dois são rastreados neste repo,
# e gitignore não alcança arquivo no índice — esse caso se resolve com verify
# escopado por pathspec, não com padrão de ignore.
.multica/
# Worktrees que o Claude Code cria no checkout ao isolar trabalho. Os diretórios
# de destino vivem no scratchpad da sessão e são apagados com ela; o que sobra
# aqui é registro administrativo não-rastreado, que reaparece a cada sessão.
# Sem esta linha ele derruba o pré-voo de árvore limpa, produz acerto falso em
# varredura que lê o working tree em vez de `git ls-files` e seria varrido para
# dentro de um commit por um `git add -A`. Ver TECH-432 e TECH-529.
.claude/worktrees/
.agent_context/
.claude/skills/multica-*/
.agents/skills/multica-*/
